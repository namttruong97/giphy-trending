```
    project
    │   tsconfig.json
    │
    └───src
    │   │
    │   └─── assets: icons, images, ...
    │   │
    │   └─── components: small component to re-used
    │   │
    │   └─── layouts: routing container
    │   │
    │   └─── models: structure of entities
    │   │
    │   └─── pages: react-admin router
    │   │
    │   └─── services: connect to BE by Axios
    │   │
    │   └─── store: Global store to save data like Redux
    │   │
    │   └─── styles: custom theme
    │   │
    │   └─── utils: helper function
```
